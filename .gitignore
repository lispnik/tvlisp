# build artifact
/tvlisp

# fasl caches
*.fasl
.cache/

# ocicl download cache (restored from ocicl.csv by `ocicl install`)
/ocicl/

# NOTE: ocicl.csv (the dependency pin manifest) IS committed.
# systems/tvision is a committed symlink to the sibling ../tvision project
# (tvision is not on ocicl).  Ignore everything else under systems/.
/systems/*
!/systems/tvision
